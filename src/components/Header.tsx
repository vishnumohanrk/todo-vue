import { Text } from '@chakra-ui/core';
import React from 'react';

const Header: React.FC = () => (
  <Text as="h1" textAlign="center" fontSize={['4xl']} fontWeight={['bold']}>
    #todo
  </Text>
);

export default Header;
